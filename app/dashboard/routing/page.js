export default function RoutingPage() {
  return (
    <div className="px-6 py-8">
      <h1 className="text-xl font-semibold text-white">Routing</h1>
      <p className="mt-1 text-sm text-zinc-500">Send invitees to the right booking page based on rules.</p>
      <div className="mt-10 rounded-xl border border-dashed border-white/[0.08] py-16 text-center text-sm text-zinc-500">
        Configure routing forms in a future update.
      </div>
    </div>
  );
}
