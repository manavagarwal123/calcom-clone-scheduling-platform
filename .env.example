# Local development (SQLite — works offline, no Neon required)
DATABASE_URL="file:./prisma/dev.db"

# Optional: use Neon / Postgres instead (change prisma/schema.prisma provider to postgresql)
# DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"
